# Allowlist for secret detection
rules:
  # Default placeholders that are not real secrets
  - pattern: "password=password"

  # Example keys in test files
  - pattern: "EXAMPLE_API_KEY"
  - pattern: "AKIAIOSFODNN7EXAMPLE"
  - pattern: "dummy_secret_key_for_testing"
